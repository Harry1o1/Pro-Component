tasks:
  - name: Django
    command: cd django && sudo apt-get update && sudo apt-get install python3 && pip install -r requirements.txt && python manage.py runserver
  - name: Nextjs
    command: cd nextjs && npm i && npm run dev
    openMode: split-right
                                                                                                                                                                                                                    